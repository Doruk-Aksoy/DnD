enum {
    DND_FLASK_LIFE_SMALL,
    DND_FLASK_LIFE_MEDIUM,
    DND_FLASK_LIFE_LARGE,
    DND_FLASK_LIFE_GRAND,
    DND_FLASK_LIFE_EXQUISITE
};

Actor Flask1_Type : Ammo {
    inventory.amount 1
	inventory.maxamount 100
    ammo.backpackamount 0
    +IGNORESKILL
}
Actor Flask2_Type : Ammo {
    inventory.amount 1
	inventory.maxamount 100
    ammo.backpackamount 0
    +IGNORESKILL
}

Actor Flask1_CurrentCharges : Ammo {
    inventory.maxamount 100
    inventory.amount 1
    ammo.backpackamount 0
    +IGNORESKILL
}

Actor Flask2_CurrentCharges : Ammo {
    inventory.maxamount 100
    inventory.amount 1
    ammo.backpackamount 0
    +IGNORESKILL
}

Actor Flask1_TicCounter : Ammo {
    inventory.maxamount 100
    inventory.amount 1
    ammo.backpackamount 0
    +IGNORESKILL
}

Actor Flask2_TicCounter : Ammo {
    inventory.maxamount 100
    inventory.amount 1
    ammo.backpackamount 0
    +IGNORESKILL
}

Actor Flask1_CritCooldown : PowerProtection {
    damagefactor "normal", 1.0
    powerup.duration 7 // 0.2 sec cd
}

Actor Flask2_CritCooldown : PowerProtection {
    damagefactor "normal", 1.0
    powerup.duration 7 // 0.2 sec cd
}

Actor FlaskSoundPlayer : CustomInventory {
	States {
		Pickup:
			TNT1 A 0 A_PlaySound("Items/FlaskDrop")
			TNT1 A 0 A_SetBlend("25 99 25", 0.6, 17)
		Stop
	}
}

Actor FlaskDrop : InventoryBase {
	Height 24
    Radius 20
	States {
		Spawn:
			TNT1 A 0
		Goto SpawnInit
		
		SpawnLoop:
		Idle:
			TNT1 AAAAAAAAAAAAAAAA 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop

        PickupSuccess:
			TNT1 A 8 ACS_NamedExecuteAlways("DnD Flask Item Pickup", 0, mass - 1)
		stop
	}
}

Actor FlaskDrop_Life_0 : FlaskDrop {
    States {
        Spawn:
            FLS1 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS1 AAAAABBBBBCCCCC 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Life_1 : FlaskDrop {
    States {
        Spawn:
            FLS2 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS2 AAAAABBBBBCCCCC 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Life_2 : FlaskDrop {
    States {
        Spawn:
            FLS3 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS3 AAAAABBBBBCCCCC 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Life_3 : FlaskDrop {
    States {
        Spawn:
            FLS4 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS4 AAAAABBBBBCCCCC 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Life_4 : FlaskDrop {
    States {
        Spawn:
            FLS5 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS5 AAAAABBBBBCCCCC 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_1 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FLS6 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS6 AAABBBCCCDDDEEEFFFGGG 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_2 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FLS7 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS7 AAABBBCCCDDDEEEFFFGGGHHH 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_3 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FLS8 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS8 AAABBBCCCDDDEEEFFF 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_4 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FLS9 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FLS9 AAABBBCCCDDDEEEFFFGGG 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_5 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FLS8 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL10 AAABBBCCCDDDEEEFFFGGG 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_6 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL11 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL11 AAABBBCCCDDDEEEFFFGGGHHH 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_7 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL12 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL12 AAABBBCCCDDDEEEFFFGGG 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_8 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL13 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL13 AAABBBCCCDDDEEE 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_9 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL14 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL14 AAABBBCCCDDD 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_10 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL15 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL15 AAA 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}

Actor FlaskDrop_Utility_11 : FlaskDrop {
    Scale 0.9
    States {
        Spawn:
            FL16 A 0
        Goto SpawnInit

        SpawnLoop:
        Idle:
			FL16 AAA 1 Bright A_LookEx(LOF_NOSOUNDCHECK, 0, item_look_dist, 0, 360, "CheckPickup")
		Loop
    }
}